name: flutter_auto_echo_cancellation_websocket_example
description: Demonstrates how to use the flutter_auto_echo_cancellation_websocket plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_auto_echo_cancellation_websocket:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
